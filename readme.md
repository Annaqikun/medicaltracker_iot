# Quick Start

Make sure you have `sqlite3` installed. Simply run `python3 database_set.py` and `python3 sample_data.py` and shout **"yay"** when the terminal doesn't explode.

```text
 |\__/,|   (`\
 |_ _  |.--.) )
 ( T   )     /
(((^_(((/(((_/
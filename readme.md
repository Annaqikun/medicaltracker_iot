# Quick Start

Make sure you have `sqlite3` installed and `cd` to the project root. Then, simply run `python3 database_set.py` and `python3 sample_data.py` and shout **"yay"** when the terminal doesn't explode.

```text
 |\__/,|   (`\
 |_ _  |.--.) )
 ( T   )     /
(((^_(((/(((_/